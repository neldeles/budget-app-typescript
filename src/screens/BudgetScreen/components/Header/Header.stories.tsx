import { Meta } from "@storybook/react";
import { Header } from ".";

export default {
  title: "components/Header",
  component: Header,
} as Meta;

// export const Default = () => <Header />;
export const Default = () => <Header />;
